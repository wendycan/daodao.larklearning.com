---
pageable: true
per_page: 10
title: 所有故事
date: 2012-01-01
---

.blogs.wrap
  .row
    .large-12.columns
      %ul.large-block-grid-4
        - page_articles.each_with_index do |article, i|
          %li
            %h3
              = link_to article.title, article, :class => 'green-link'
            %p.date.primary-link
              = article.date.strftime('%b %e')
            / = article.body
            %p= article.summary(150)
  .row
    .large-12.columns
      - if paginate && num_pages > 1
        %p
          Page
          = page_number
          of
          = num_pages

        - if prev_page
          %p
            = link_to 'Previous page', prev_page

      - if paginate
        - if next_page
          %p
            = link_to 'Next page', next_page
